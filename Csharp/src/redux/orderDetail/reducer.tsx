import actions from "./actions";

const initAuth = {};
const OrderDetailReducer = (state: any = initAuth, action: any) => {
  switch (action.type) {
    default:
      return state;
  }
};
export default OrderDetailReducer;
